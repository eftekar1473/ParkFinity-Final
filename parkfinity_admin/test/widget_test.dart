// Smoke test placeholder. The admin app requires dotenv + Supabase init at
// startup, so a full pumpWidget boot isn't meaningful in a unit test.
import 'package:flutter_test/flutter_test.dart';

void main() {
  test('placeholder', () {
    expect(1 + 1, 2);
  });
}
